FROM scratch
LABEL org.opencontainers.image.title="secops-supply-chain-fixture"
LABEL org.opencontainers.image.description="Non-runtime OCI fixture used only to test build, scan, SBOM, signing and attestation mechanics"
COPY payload.txt /payload.txt
